pipeline{
    agent any

    parameters{
        (name: '' , defaultValue: '')
    }

    environment{
        ECR_REPO_URL=
        ECR_APP_NAME=
        APP_NAME=
        CRED_ID=
        ECS_CLUSTER_NAME=
        ECS_SVC_NAME=
    }

    stages{
        stage("build image and push to ecr"){
            steps{
                script{

                }
            }
        }
        stage(""){
            steps{

            }
        }
    }
    
}